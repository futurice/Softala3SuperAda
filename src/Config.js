export const pointsPerMinute = 10;
export const pointsPerWord = 5;
export const pointsCompleted = 100;
export const maxMinutes = 5;

export const timelimit = maxMinutes * 60; //__DEV__ ? 100 : maxMinutes * 60;
